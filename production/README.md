# Production

Production-management artifacts produced by framework skills:

- `sprints/` — sprint plans (`/sprint-plan`)
- `milestones/` — milestone definitions & reviews (`/milestone-review`)
- `releases/` — launch checklists (`/launch-checklist`)
