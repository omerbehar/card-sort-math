source "https://rubygems.org"

# Tooling for TestFlight tester management (fastlane pilot / App Store Connect API).
# iOS/Apple tooling only — not needed to build or test the Godot game itself.
gem "fastlane"
